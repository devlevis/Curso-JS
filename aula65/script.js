//ForEach:
//Formula base:
//function(valor, indice, array)
//Serve apenas para iterar sobre os valores
const a1 = [10, 20, 30];
let total = 0;
a1.forEach(valor => {
    total += valor
});
console.log(total)